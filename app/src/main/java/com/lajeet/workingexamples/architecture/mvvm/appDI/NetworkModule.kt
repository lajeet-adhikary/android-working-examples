package com.lajeet.workingexamples.architecture.mvvm.appDI

class NetworkModule {
    /**
     * Provide dependencies and objects like:
     * 1. Retrofit builder (using Okhttp & interceptors which can be used for logging, caching, etc.)
     * 2. Api Service of the interfaces of the features and create retrofit instances which can be passed to the repository to call apis
     * */
}